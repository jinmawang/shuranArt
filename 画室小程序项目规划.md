# 画室小程序项目规划

## 一、项目概述

### 1.1 项目背景
个人画室小程序，主要用于画室宣传和招生，核心场景是在开学季、暑假前、寒假前、国庆等节点做活动推广。

### 1.2 核心功能模块
| 模块 | 说明 | 优先级 |
|------|------|--------|
| 画室介绍 | 画室环境、教学理念、荣誉资质 | P1 |
| 老师介绍 | 师资力量、教学经验、作品展示 | P1 |
| 课程介绍 | 课程体系、价格、适合人群 | P1 |
| 活动介绍 | 限时活动、优惠信息（核心页面） | P0 |
| 分享裂变 | 分享激励机制 | P0 |

---

## 二、技术架构选型

### 2.1 推荐方案：微信云开发（强烈推荐）

**原因：**
1. **零后端运维** - 你是前端开发者，云开发让你无需搭建服务器
2. **免费额度** - 2026年微信AI小程序成长计划提供6个月免费云开发环境
3. **原生打通微信能力** - 分享、支付、消息推送等免鉴权直接调用
4. **学习成本低** - 云函数用 JavaScript/TypeScript 编写

**云开发核心能力：**
```
┌─────────────────────────────────────────┐
│           微信小程序前端                  │
└────────────────┬────────────────────────┘
                 │ 免鉴权调用
┌────────────────▼────────────────────────┐
│              云开发                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 云函数   │ │ 云数据库  │ │ 云存储   │ │
│  │(Node.js) │ │(MongoDB) │ │(图片/文件)│ │
│  └──────────┘ └──────────┘ └──────────┘ │
└─────────────────────────────────────────┘
```

### 2.2 备选方案：自建后端（你已有腾讯云服务器）

如果你更想练习后端技术，可以使用自己的服务器：

| 技术栈 | 说明 | 学习难度 |
|--------|------|----------|
| Node.js + Express/Koa | 前端友好，JS全栈 | ⭐⭐ |
| Python + FastAPI | 简洁高效，AI相关功能方便扩展 | ⭐⭐ |
| Go + Gin | 高性能，适合学习运维 | ⭐⭐⭐ |

**注意：** 自建后端需要：
- HTTPS 证书配置（小程序强制要求）
- 域名备案
- 服务器运维

### 2.3 推荐架构决策

```
第一阶段（MVP快速上线）：使用云开发
     ↓
第二阶段（业务成熟后）：核心逻辑迁移到自建服务器（可选）
```

---

## 三、分享抽奖裂变方案设计

### 3.1 核心玩法

```
用户分享活动页 → 好友点击进入 → 分享者获得1次抽奖机会
                                        ↓
                                   点击抽奖
                                        ↓
                              随机抽中奖品（积分/体验课/画材礼包）
                                        ↓
                              实物奖品线下到店领取
```

### 3.2 奖品池设计

| 奖品类型 | 奖品内容 | 中奖概率 | 领取方式 |
|----------|----------|----------|----------|
| 积分（小） | 5积分 | 40% | 自动发放 |
| 积分（中） | 20积分 | 25% | 自动发放 |
| 积分（大） | 50积分 | 15% | 自动发放 |
| 积分（特大） | 100积分 | 5% | 自动发放 |
| 体验课 | 免费体验课1节 | 10% | 线下领取 |
| 画材礼包 | 画材小礼包 | 5% | 线下领取 |

**概率调整建议：**
- 初期可提高大奖概率吸引用户
- 根据成本和效果动态调整
- 保证每次都能中奖，增强用户体验

### 3.3 积分用途

| 积分数量 | 可兑换内容 |
|----------|------------|
| 50积分 | 课程抵扣券 20元 |
| 100积分 | 课程抵扣券 50元 |
| 200积分 | 免费体验课 1节 |
| 500积分 | 画材礼包（中） |

### 3.4 防刷机制

| 限制类型 | 规则 |
|----------|------|
| 每日分享上限 | 每人每天最多获得 5 次抽奖机会 |
| 同一好友限制 | 同一好友点击仅计 1 次（去重） |
| 抽奖有效期 | 抽奖机会 7 天内有效 |
| 实物领取 | 需到店出示兑换码，核销后失效 |

### 3.5 微信分享合规说明

**允许的做法：**
- ✅ 分享后获得抽奖机会
- ✅ 积分/实物奖励
- ✅ 线下领取奖品

**禁止的行为：**
- ❌ 强制转发才能查看活动内容
- ❌ 虚假奖品诱导
- ❌ 分享后才能解锁页面功能

### 3.6 分享追踪技术实现

```javascript
// 分享时携带分享者ID和活动ID
onShareAppMessage() {
  return {
    title: '【分享抽奖】XX画室暑期班，分享赢好礼！',
    path: `/pages/activity/detail?shareFrom=${this.userId}&activityId=${this.activityId}`,
    imageUrl: '/images/share-cover.jpg'
  }
}

// 分享到朋友圈
onShareTimeline() {
  return {
    title: '【限时活动】XX画室暑期班报名优惠中',
    query: `shareFrom=${this.userId}&activityId=${this.activityId}`,
    imageUrl: '/images/share-cover.jpg'
  }
}

// 被分享者进入页面时，给分享者增加抽奖机会
onLoad(options) {
  if (options.shareFrom && options.shareFrom !== this.userId) {
    wx.cloud.callFunction({
      name: 'recordShareAndAddLottery',
      data: {
        sharerId: options.shareFrom,
        visitorId: this.userId,
        activityId: options.activityId
      }
    })
  }
}
```

### 3.7 抽奖交互流程

```
┌─────────────────────────────────────────────────────┐
│                    用户中心                          │
│  ┌─────────────────────────────────────────────┐   │
│  │  🎰 我的抽奖机会: 3 次          [去抽奖]     │   │
│  └─────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────┐   │
│  │  💰 我的积分: 150              [去兑换]      │   │
│  └─────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────┐   │
│  │  🎁 待领取奖品: 2 个           [查看]        │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│                   抽奖页面                           │
│                                                     │
│            ┌───┐ ┌───┐ ┌───┐                       │
│            │5分│ │体验│ │20分│                      │
│            └───┘ └───┘ └───┘                       │
│            ┌───┐ ┌───┐ ┌───┐                       │
│            │画材│ │抽奖│ │50分│                      │
│            └───┘ └───┘ └───┘                       │
│            ┌───┐ ┌───┐ ┌───┐                       │
│            │100│ │5分│ │20分│                       │
│            └───┘ └───┘ └───┘                       │
│                                                     │
│         剩余抽奖机会: 3 次                           │
│                                                     │
│    [分享给好友获得更多抽奖机会]                        │
└─────────────────────────────────────────────────────┘
```

---

## 四、数据库设计

### 4.1 核心集合（云开发 MongoDB）

```javascript
// 用户表 users
{
  _id: "openid",
  nickName: "用户昵称",
  avatarUrl: "头像",
  phone: "手机号",
  points: 0,              // 当前积分
  lotteryChances: 0,      // 剩余抽奖次数
  totalShares: 0,         // 累计分享次数
  totalLotteries: 0,      // 累计抽奖次数
  createdAt: Date,
  updatedAt: Date
}

// 分享记录表 share_records
{
  _id: "自动生成",
  sharerId: "分享者openid",
  visitorId: "访客openid",
  activityId: "活动ID",
  createdAt: Date,
  lotteryGranted: true    // 是否已发放抽奖机会
}

// 抽奖记录表 lottery_records
{
  _id: "自动生成",
  oddate: "用户openid",
  odprizeId: "奖品ID",
  prizeName: "50积分",
  prizeType: "points",    // points/experience/gift
  prizeValue: 50,         // 积分数量或奖品价值
  status: "pending",      // pending/claimed/expired
  claimCode: "ABCD1234",  // 实物奖品领取码
  createdAt: Date,
  claimedAt: Date,        // 领取时间
  expireAt: Date          // 过期时间（实物奖品30天内领取）
}

// 奖品配置表 prizes
{
  _id: "奖品ID",
  name: "50积分",
  type: "points",         // points/experience/gift
  value: 50,              // 积分数量
  probability: 15,        // 中奖概率（百分比）
  stock: -1,              // 库存，-1表示无限
  description: "可用于兑换课程优惠",
  icon: "/images/prize-points.png",
  needClaim: false,       // 是否需要线下领取
  status: "active"        // active/inactive
}

// 活动表 activities
{
  _id: "活动ID",
  title: "暑期班报名优惠",
  desc: "活动描述",
  coverImg: "封面图",
  startTime: Date,
  endTime: Date,
  dailyShareLimit: 5,     // 每日分享获得抽奖次数上限
  lotteryExpireDays: 7,   // 抽奖机会有效天数
  status: "active"        // active/ended
}

// 积分兑换商品表 exchange_items
{
  _id: "商品ID",
  name: "课程抵扣券50元",
  pointsCost: 100,        // 所需积分
  stock: 100,             // 库存
  description: "报名任意课程可抵扣50元",
  image: "/images/coupon-50.png",
  needClaim: true,        // 是否需要线下领取
  status: "active"
}

// 兑换记录表 exchange_records
{
  _id: "自动生成",
  userId: "用户openid",
  itemId: "商品ID",
  itemName: "课程抵扣券50元",
  pointsCost: 100,
  claimCode: "EXCH1234",  // 兑换码
  status: "pending",      // pending/claimed/expired
  createdAt: Date,
  claimedAt: Date,
  expireAt: Date
}

// 课程表 courses
{
  _id: "课程ID",
  name: "素描基础班",
  desc: "课程描述",
  price: 2000,
  duration: "3个月",
  suitableFor: "零基础学员",
  coverImg: "封面图"
}

// 老师表 teachers
{
  _id: "老师ID",
  name: "张老师",
  title: "主讲教师",
  intro: "个人简介",
  avatar: "头像",
  works: ["作品1URL", "作品2URL"]
}
```

### 4.2 数据库索引建议

```javascript
// share_records 索引
{ sharerId: 1, visitorId: 1, activityId: 1 }  // 防重复
{ sharerId: 1, createdAt: -1 }                 // 查询用户分享记录

// lottery_records 索引
{ oddate: 1, createdAt: -1 }                   // 查询用户抽奖记录
{ oddate: 1, status: 1 }                       // 查询待领取奖品
{ claimCode: 1 }                               // 核销时查询

// exchange_records 索引
{ userId: 1, createdAt: -1 }                   // 查询用户兑换记录
{ claimCode: 1 }                               // 核销时查询
```

---

## 五、页面结构设计

```
pages/
├── index/              # 首页（画室介绍）
├── teachers/           # 老师介绍
│   ├── list           # 老师列表
│   └── detail         # 老师详情
├── courses/           # 课程介绍
│   ├── list           # 课程列表
│   └── detail         # 课程详情
├── activity/          # 活动页面（核心）
│   ├── list           # 活动列表
│   └── detail         # 活动详情（可分享）
├── lottery/           # 抽奖模块
│   ├── index          # 抽奖主页（九宫格抽奖）
│   └── records        # 抽奖记录
├── user/              # 用户中心
│   ├── index          # 我的（抽奖机会/积分/待领取）
│   ├── points         # 我的积分
│   ├── exchange       # 积分兑换商城
│   ├── prizes         # 我的奖品（待领取）
│   └── shares         # 分享记录
```

---

## 六、开发阶段规划

### 第一阶段：基础框架（MVP）
- [ ] 创建小程序项目
- [ ] 开通云开发环境
- [ ] 搭建基础页面结构
- [ ] 实现画室介绍页
- [ ] 实现老师介绍页
- [ ] 实现课程介绍页

### 第二阶段：核心功能（抽奖裂变）
- [ ] 活动详情页开发
- [ ] 分享功能实现（带参数追踪）
- [ ] 分享记录云函数
- [ ] 九宫格抽奖组件开发
- [ ] 抽奖云函数（概率控制）
- [ ] 用户中心开发
- [ ] 奖品领取码生成与展示

### 第三阶段：积分商城
- [ ] 积分兑换商城页面
- [ ] 积分兑换云函数
- [ ] 兑换记录管理

### 第四阶段：运营优化
- [ ] 分享海报生成（canvas）
- [ ] 消息订阅推送（奖品到期提醒）
- [ ] 数据统计后台（可用云开发控制台）
- [ ] 奖品核销功能（管理端小程序或H5）

---

## 七、后端服务实现指南（云开发）

### 7.1 开通云开发

```bash
1. 微信开发者工具 → 云开发控制台 → 开通
2. 选择"按量计费"（有免费额度）
3. 初始化云开发环境
```

### 7.2 云函数示例

**1. 记录分享并发放抽奖机会 recordShareAndAddLottery:**
```javascript
// cloudfunctions/recordShareAndAddLottery/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()
const _ = db.command

exports.main = async (event, context) => {
  const { sharerId, visitorId, activityId } = event

  // 1. 不能给自己加抽奖机会
  if (sharerId === visitorId) {
    return { success: false, msg: '不能给自己助力' }
  }

  // 2. 检查是否已记录过（同一访客对同一分享者同一活动只算一次）
  const existing = await db.collection('share_records')
    .where({ sharerId, visitorId, activityId })
    .get()

  if (existing.data.length > 0) {
    return { success: false, msg: '已助力过' }
  }

  // 3. 检查分享者今日是否达到上限
  const today = new Date()
  today.setHours(0, 0, 0, 0)
  const todayShares = await db.collection('share_records')
    .where({
      sharerId,
      createdAt: _.gte(today)
    })
    .count()

  const activity = await db.collection('activities').doc(activityId).get()
  const dailyLimit = activity.data.dailyShareLimit || 5

  if (todayShares.total >= dailyLimit) {
    // 记录分享但不发放抽奖机会
    await db.collection('share_records').add({
      data: {
        sharerId, visitorId, activityId,
        createdAt: db.serverDate(),
        lotteryGranted: false
      }
    })
    return { success: true, msg: '今日抽奖机会已达上限', lotteryAdded: false }
  }

  // 4. 记录分享并发放抽奖机会
  await db.collection('share_records').add({
    data: {
      sharerId, visitorId, activityId,
      createdAt: db.serverDate(),
      lotteryGranted: true
    }
  })

  await db.collection('users').doc(sharerId).update({
    data: {
      lotteryChances: _.inc(1),
      totalShares: _.inc(1)
    }
  })

  return { success: true, msg: '获得1次抽奖机会', lotteryAdded: true }
}
```

**2. 执行抽奖 doLottery:**
```javascript
// cloudfunctions/doLottery/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()
const _ = db.command

// 生成随机领取码
function generateClaimCode() {
  const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'
  let code = ''
  for (let i = 0; i < 8; i++) {
    code += chars.charAt(Math.floor(Math.random() * chars.length))
  }
  return code
}

exports.main = async (event, context) => {
  const wxContext = cloud.getWXContext()
  const openid = wxContext.OPENID

  // 1. 检查用户抽奖机会
  const user = await db.collection('users').doc(openid).get()
  if (!user.data || user.data.lotteryChances <= 0) {
    return { success: false, msg: '没有抽奖机会' }
  }

  // 2. 扣减抽奖机会
  await db.collection('users').doc(openid).update({
    data: {
      lotteryChances: _.inc(-1),
      totalLotteries: _.inc(1)
    }
  })

  // 3. 获取奖品池并按概率抽奖
  const prizesRes = await db.collection('prizes')
    .where({ status: 'active' })
    .get()
  const prizes = prizesRes.data

  // 根据概率抽奖
  const random = Math.random() * 100
  let cumulative = 0
  let selectedPrize = null

  for (const prize of prizes) {
    cumulative += prize.probability
    if (random <= cumulative) {
      selectedPrize = prize
      break
    }
  }

  // 兜底：如果没选中，选最低奖
  if (!selectedPrize) {
    selectedPrize = prizes.find(p => p.type === 'points' && p.value === 5) || prizes[0]
  }

  // 4. 检查库存（如果有库存限制）
  if (selectedPrize.stock !== -1 && selectedPrize.stock <= 0) {
    // 库存不足，降级到积分奖品
    selectedPrize = prizes.find(p => p.type === 'points') || prizes[0]
  }

  // 5. 扣减库存
  if (selectedPrize.stock !== -1) {
    await db.collection('prizes').doc(selectedPrize._id).update({
      data: { stock: _.inc(-1) }
    })
  }

  // 6. 记录中奖信息
  const expireAt = new Date()
  expireAt.setDate(expireAt.getDate() + 30) // 30天内领取

  const lotteryRecord = {
    oddate: openid,
    odprizeId: selectedPrize._id,
    prizeName: selectedPrize.name,
    prizeType: selectedPrize.type,
    prizeValue: selectedPrize.value,
    status: selectedPrize.needClaim ? 'pending' : 'claimed',
    claimCode: selectedPrize.needClaim ? generateClaimCode() : null,
    createdAt: db.serverDate(),
    claimedAt: selectedPrize.needClaim ? null : db.serverDate(),
    expireAt: selectedPrize.needClaim ? expireAt : null
  }

  await db.collection('lottery_records').add({ data: lotteryRecord })

  // 7. 如果是积分，直接发放
  if (selectedPrize.type === 'points') {
    await db.collection('users').doc(openid).update({
      data: { points: _.inc(selectedPrize.value) }
    })
  }

  return {
    success: true,
    prize: {
      name: selectedPrize.name,
      type: selectedPrize.type,
      value: selectedPrize.value,
      icon: selectedPrize.icon,
      needClaim: selectedPrize.needClaim,
      claimCode: lotteryRecord.claimCode
    }
  }
}
```

**3. 积分兑换 exchangePoints:**
```javascript
// cloudfunctions/exchangePoints/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()
const _ = db.command

function generateClaimCode() {
  const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'
  let code = 'EX'
  for (let i = 0; i < 6; i++) {
    code += chars.charAt(Math.floor(Math.random() * chars.length))
  }
  return code
}

exports.main = async (event, context) => {
  const wxContext = cloud.getWXContext()
  const openid = wxContext.OPENID
  const { itemId } = event

  // 1. 获取商品信息
  const item = await db.collection('exchange_items').doc(itemId).get()
  if (!item.data || item.data.status !== 'active') {
    return { success: false, msg: '商品不存在或已下架' }
  }

  // 2. 检查库存
  if (item.data.stock <= 0) {
    return { success: false, msg: '库存不足' }
  }

  // 3. 检查用户积分
  const user = await db.collection('users').doc(openid).get()
  if (!user.data || user.data.points < item.data.pointsCost) {
    return { success: false, msg: '积分不足' }
  }

  // 4. 扣减积分和库存
  await db.collection('users').doc(openid).update({
    data: { points: _.inc(-item.data.pointsCost) }
  })

  await db.collection('exchange_items').doc(itemId).update({
    data: { stock: _.inc(-1) }
  })

  // 5. 创建兑换记录
  const expireAt = new Date()
  expireAt.setDate(expireAt.getDate() + 30)

  const record = {
    userId: openid,
    itemId: itemId,
    itemName: item.data.name,
    pointsCost: item.data.pointsCost,
    claimCode: item.data.needClaim ? generateClaimCode() : null,
    status: item.data.needClaim ? 'pending' : 'claimed',
    createdAt: db.serverDate(),
    expireAt: item.data.needClaim ? expireAt : null
  }

  await db.collection('exchange_records').add({ data: record })

  return {
    success: true,
    msg: '兑换成功',
    claimCode: record.claimCode,
    needClaim: item.data.needClaim
  }
}
```

**4. 核销奖品（管理端使用）verifyPrize:**
```javascript
// cloudfunctions/verifyPrize/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()

exports.main = async (event, context) => {
  const { claimCode, type } = event // type: 'lottery' 或 'exchange'

  const collection = type === 'lottery' ? 'lottery_records' : 'exchange_records'

  // 1. 查找记录
  const record = await db.collection(collection)
    .where({ claimCode, status: 'pending' })
    .get()

  if (record.data.length === 0) {
    return { success: false, msg: '兑换码无效或已使用' }
  }

  // 2. 检查是否过期
  const item = record.data[0]
  if (item.expireAt && new Date(item.expireAt) < new Date()) {
    return { success: false, msg: '兑换码已过期' }
  }

  // 3. 核销
  await db.collection(collection).doc(item._id).update({
    data: {
      status: 'claimed',
      claimedAt: db.serverDate()
    }
  })

  return {
    success: true,
    msg: '核销成功',
    prize: type === 'lottery' ? item.prizeName : item.itemName
  }
}
```

### 7.3 如果使用自建服务器

你的腾讯云服务器可以这样使用：

```
1. 安装 Node.js 环境
2. 使用 Express/Koa 搭建 API
3. 使用 MongoDB/MySQL 作为数据库
4. 配置 Nginx + HTTPS 证书
5. 在小程序后台配置服务器域名
```

**但建议：** 先用云开发快速验证业务，等熟悉后端后再考虑迁移。

---

## 八、成本预估

### 云开发方案（推荐）

| 项目 | 费用 |
|------|------|
| 云开发环境 | 免费（6个月成长计划）/基础版约20元/月 |
| 小程序认证 | 个人版免费，企业版300元/年 |
| 域名 | 你已有 |

**总计：** 初期几乎0成本

### 自建服务器方案

| 项目 | 费用 |
|------|------|
| 云服务器 | 你已有 |
| SSL证书 | 免费（Let's Encrypt） |
| 域名 | 你已有 |
| 运维时间 | 较多 |

---

## 九、个人主体说明

个人小程序完全支持本方案：
- ✅ 分享抽奖机制（无需支付接口）
- ✅ 积分系统
- ✅ 云开发后端
- ✅ 线下领取实物奖品

**优势：**
- 零成本启动
- 无需企业资质
- 合规风险低

---

## 十、下一步行动

1. **注册小程序** - 微信公众平台注册个人小程序
2. **开通云开发** - 申请AI小程序成长计划获取免费资源
3. **搭建项目骨架** - 使用微信开发者工具创建项目
4. **开发基础页面** - 从画室介绍页开始

---

## 参考资源

- [微信云开发官网](https://cloud.weixin.qq.com/cloudbase)
- [2026微信AI小程序成长计划](https://developers.weixin.qq.com/community/develop/article/doc/000846597985681fa774c842e66813)
- [腾讯云开发](https://tcb.cloud.tencent.com/)
- [微信小程序分享API文档](https://developers.weixin.qq.com/miniprogram/dev/reference/api/Page.html#onShareAppMessage-Object-object)
